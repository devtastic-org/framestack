const DEFAULT_FRAME_SIZES = {
  small: { width: 360, height: 600 },
  medium: { width: 600, height: 600 },
  large: { width: 1200, height: 800 },
};

export const DEFAULT_FORMSTACK_CONFIG = {
  sizes: DEFAULT_FRAME_SIZES,
};

export const DEFAULT_FORM_CONFIG = {
  background: "white",
  shadow: true,
  position: {
    top: "auto",
    right: 10,
    bottom: 10,
    left: "auto",
  },
};
